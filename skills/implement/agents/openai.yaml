interface:
  display_name: "Implement"
  short_description: "Build work with an isolated implementer and review"
  default_prompt: "Use $implement to build the requested work with an isolated implementer and independent review."
policy:
  allow_implicit_invocation: false
