interface:
  display_name: "To Tickets"
  short_description: "Turn plans into tracer-bullet tickets"
  default_prompt: "Use $to-tickets to turn this plan or conversation into tracer-bullet tickets."
policy:
  allow_implicit_invocation: false
